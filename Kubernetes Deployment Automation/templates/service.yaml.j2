apiVersion: v1
kind: Service
metadata:
    name: {{ deployment_name }}-service

spec:
    selector:
        app: {{ deployment_name }}
    
    ports:
    - port: {{ service_port }}
      targetPort: {{ container_port }}
      
    type: ClusterIP
